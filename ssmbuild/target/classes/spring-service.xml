<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       https://www.springframework.org/schema/aop/spring-aop.xsd">



    <!--扫描service下的包-->
    <context:component-scan base-package="indi.wgx.service"/>

    <!--将 BookServiceImpl 即所有的业务类都注册到IOC容器中，可以通过配置，或通过注解实现-->
    <bean id="bookServiceImpl" class="indi.wgx.service.BookServiceImpl">
        <!--能引用 bookMapper 是因为在 spring-dao.xml 中配置了dao接口扫描，会将 dao 下的接口注入到spring容器中，并且引用名称默认是接口的首字母小写-->
        <!--spring-dao.xml 和 spring-service.xml 放置在同一个上下文环境中-->
        <property name="bookMapper" ref="bookMapper"/>
    </bean>

    <!--声明式事务, 配置事务管理器-->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <!--注入数据库连接池-->
        <property name="dataSource" ref="dataSource"/>
    </bean>

</beans>